-- roblox voicechat filtering bypass

local Players = game:GetService("Players")
local VoiceChatService = game:GetService("VoiceChatService")
local RunService = game:GetService("RunService")
local CoreGui = game:GetService("CoreGui")
local ReplicatedStorage = game:GetService("ReplicatedStorage")

local LocalPlayer = Players.LocalPlayer
local vcConnection = nil

local function safeSpoof()
    pcall(function()
        LocalPlayer:SetAttribute("AgeVerified", true)
        LocalPlayer:SetAttribute("VoiceChatEnabled", true)
        LocalPlayer:SetAttribute("IsVoiceChatUser", true)
        LocalPlayer:SetAttribute("VCReportable", false)
        
        if LocalPlayer:FindFirstChild("PlayerGui") then
            local voiceGui = LocalPlayer.PlayerGui:FindFirstChild("VoiceChat") or LocalPlayer.PlayerGui:FindFirstChildWhichIsA("VoiceChatGui")
            if voiceGui then voiceGui.Enabled = true end
        end
        CoreGui:FindFirstChild("VoiceChat").Enabled = true
    end)
end

local function enhancedBypass()
    local mt = getrawmetatable(VoiceChatService)
    local oldNamecall = mt.__namecall
    setreadonly(mt, false)
    
    mt.__namecall = newcclosure(function(self, ...)
        local method = getnamecallmethod()
        local args = {...}
        
        if method:find("Mute") or method:find("Unmute") or method:find("Settings") or 
           method:find("Toggle") or method == "OpenVoiceMenu" then
            return oldNamecall(self, ...)
        end
        
        -- to bypass checks
        if method:find("Init") or method:find("VoiceChat") or method == "GetVoiceChatState" then
            safeSpoof()
            setthreadidentity(8)
            local result = oldNamecall(self, ...)
            safeSpoof()  -- Double apply
            return result
        end
        return oldNamecall(self, ...)
    end)
    setreadonly(mt, true)

    hookfunction(VoiceChatService.IsVoiceEnabledForUserIdAsync, function() return true end)
    hookfunction(VoiceChatService.GetVoiceChatState, function() return Enum.VoiceChatState.Enabled end)
    
    -- Minimal, targeted cleanup
    for _, obj in ipairs(game:GetDescendants()) do
        if obj.Name:match("VoiceBan") or obj.Name:match("VCTelemetry") or obj.Name:match("ReportVoice") then
            pcall(function() obj:Destroy() end)
        end
    end

    if vcConnection then vcConnection:Disconnect() end
    vcConnection = RunService.Heartbeat:Connect(function()
        if math.random(1, 30) == 1 then
            pcall(safeSpoof)
        end
    end)

    pcall(function()
        if VoiceChatService:FindFirstChild("VoiceReportService") then
            VoiceChatService.VoiceReportService.Disabled = true
        end
    end)

    print("enhanced vc bypass is activated bro")
end

enhancedBypass()

LocalPlayer.CharacterAdded:Connect(function()
    task.wait(2)
    safeSpoof()
end)
